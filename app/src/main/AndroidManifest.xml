<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bw.qgs.qgs2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".login.ReginActivity" />
        <activity android:name=".homepage.HomeActivity" />
        <activity android:name=".wallet.WalletActivity" />
        <activity android:name=".footer.FooterActivity" />
        <activity android:name=".circle.CircleActivity" />
        <activity android:name=".homepage.fragment.particulars.GoodsParticularsActivity" />
        <activity android:name=".myaddress.AddressActivity" />
        <activity android:name=".xinzeng.XinZengActivity"></activity>
    </application>

</manifest>